spring:
  application:
    name: notif-api
  threads:
    virtual:
      enabled: true

server:
  port: ${EBAY_SOFT_NOTIF_PORT:8087}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
