spring:
  application:
    name: billing-api
  threads:
    virtual:
      enabled: true

server:
  port: ${EBAY_SOFT_BILLING_PORT:8088}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
