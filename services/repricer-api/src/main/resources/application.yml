spring:
  application:
    name: repricer-api
  threads:
    virtual:
      enabled: true

server:
  port: ${EBAY_SOFT_REPRICER_PORT:8090}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
