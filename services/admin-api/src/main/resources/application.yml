spring:
  application:
    name: admin-api
  threads:
    virtual:
      enabled: true

server:
  port: ${EBAY_SOFT_ADMIN_PORT:8089}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
