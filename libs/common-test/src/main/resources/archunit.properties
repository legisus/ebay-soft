# A clean codebase that has no violations of a rule still gives the rule
# nothing to check. ArchUnit considers that a configuration smell by default
# and fails the test. For our rules — many of which are "no class should
# ever do X" — zero candidates is the goal, so we flip the flag.
archRule.failOnEmptyShould=false
